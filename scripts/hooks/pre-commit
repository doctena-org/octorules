#!/usr/bin/env bash
# Pre-commit hook: check baked-in CDN ranges are not stale.
#
# Install: from the repo root, run:
#   ln -sf ../../scripts/hooks/pre-commit .git/hooks/pre-commit
set -euo pipefail

.venv/bin/python scripts/sync_cdn_ranges.py --check
