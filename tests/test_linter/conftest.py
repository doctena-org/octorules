"""Shared fixtures for the linter test suite.

Assertion helpers (``assert_lint``, ``assert_no_lint``) live in
``octorules.testing.lint`` so providers can import them without
duplicating the helpers in their own conftests.
"""
